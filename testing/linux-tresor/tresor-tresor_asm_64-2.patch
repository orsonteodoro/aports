--- linux-4.14-generic/arch/x86/crypto/tresor_asm.S.orig
+++ linux-4.14-generic/arch/x86/crypto/tresor_asm.S
@@ -505,12 +505,12 @@
 tresor_set_key:
 	movl		4(esp),edx
 	movl		0(edx),eax
-	movl		eax,db0
+	movq		rax,db0
 	movl		4(edx),eax
-	movl		eax,db1
+	movq		rax,db1
 	movl		8(edx),eax
-	movl		eax,db2
+	movq		rax,db2
 	movl		12(edx),eax
-	movl		eax,db3
+	movq		rax,db3
 	xorl		eax, eax
 	ret
